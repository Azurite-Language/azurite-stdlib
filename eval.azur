(import "eval/addition")
(import "eval/and")
(import "eval/or")
(import "eval/equal")
(import "eval/if")
(import "eval/defun")

(macro ("azeval" 'lang') (
    (eval/addition lang)
    (eval/and lang)
    (eval/or lang)
    (eval/equal lang)
    (eval/if lang)
    (eval/defun lang)
))
